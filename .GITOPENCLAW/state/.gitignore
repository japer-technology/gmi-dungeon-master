# OpenClaw runtime state — excluded from git
# All runtime artifacts live inside .GITOPENCLAW/state/ but only the
# audit trail (issue mappings, memory log) is committed.
# OpenClaw's internal directories are ephemeral and regenerated each run.

# OpenClaw internal directories (regenerated by the runtime)
agents/
cache/
credentials/
sandbox/
logs/

# SQLite databases (runtime-only, not portable)
*.db
*.db-journal
*.db-wal
*.db-shm

# Workspace mirror (agent reads source from repo root directly)
workspace/
